int main()
{
	//一只大象口渴了，要喝 2020 升水才能解渴，但现在只有一个深 hh 厘米，
	//底面半径为 rr 厘米的小圆桶 （hh 和 rr 都是整数）。问大象至少要喝多少桶水才会解渴。
	int r, h;
	scanf_s("%d %d", &h, &r);
	int V = (3.14/1000.0) * h * r * r;
	int num = 20/V ;
	printf("%d", num+1);
	return 0;
}
