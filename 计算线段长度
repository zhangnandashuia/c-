#include<stdio.h>
#include<math.h>
int main()
{
	int x1, x2, y1, y2;
	scanf_s("%d %d\n%d %d", &x1, &y1, &x2,&y2);
	int x = pow(x1 - x2, 2);
	int y = pow(y1 - y2, 2);
	double l = sqrt(x + y);
	printf("%.3f", l);
	return 0;
}
