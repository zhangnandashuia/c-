#include<math.h>
#include<stdio.h>
int main()
{
	int i;
	scanf_s("%d", &i);
	
	while (i != 1)
	{
		if (i % 2 == 1)
		{
			
			printf("%d*3+1=%d\n", i,3*i+1 );
			i = 3 * i + 1;

		}
		else
		{
			printf("%d/2=%d\n", i, i / 2);
			i = i / 2;
		}

	}
	printf("End");
	return 0;
}
